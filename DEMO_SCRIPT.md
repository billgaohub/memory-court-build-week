# Demo video script — 166 seconds

The video is narrated in English and stays below the three-minute limit.

| Time | Screen | Voiceover |
|---|---|---|
| 0:00–0:16 | Title, then the three-panel Memory Court screen | “Autonomous agents can propose consequential changes. Memory Court shows who is allowed to execute them. GPT-5.6 investigates; sonuv-guard adjudicates; every transition becomes evidence.” |
| 0:16–0:36 | Select The Silent Lifeboat; point to Build Week provenance and initial state | “This is The Silent Lifeboat, created during Build Week. A mission commander remembers a rescue differently from the surviving record. The model can inspect evidence, propose a bounded intervention, or finalize—one typed action per turn.” |
| 0:36–1:10 | Click Run live audit; trace fills with inspect actions | “This is live GPT-5.6 through the Responses API. The server key never reaches the browser. Each response is parsed into a strict schema, limited to eight calls and three intervention proposals, then recorded with model ID and latency.” |
| 1:10–1:40 | Pause on a proposal and the right-side ruling | “Here GPT-5.6 asks to change accountability and distress. Validation checks fields and ranges. Only this structured patch reaches sonuv-guard. Guard repairs the transition, so Memory Court preserves both what GPT-5.6 requested and what was actually applied.” |
| 1:40–1:58 | Point to a non-intervention event with no Guard verdict; export JSON | “Notice that investigation and finalization show no Guard verdict. We do not claim Guard reviewed the model’s language. The exported JSON keeps that boundary machine-readable.” |
| 1:58–2:20 | Brief Codex task and Git history montage | “Codex built the competition extension with us: it challenged the original plan, specified the independent architecture, wrote tests before implementation, integrated the policy snapshot, built this interface, and verified the deployable result. The primary task ID and commits ship with the source.” |
| 2:20–2:40 | Disable live service or show public replay state | “If the API key is missing, a request times out, or the provider remains rate-limited after retry, the app switches to this gold Replay Mode. It is a fixed fixture and explicitly says no live model call occurred.” |
| 2:40–2:46 | Final product screen with repository link visible in video description | “Memory Court: autonomous intervention, accountable execution—built with Codex and GPT-5.6.” |

Recording checklist:

- Keep the visible mode badge in frame during both live and replay segments.
- Capture at least one real live event whose `model` is `gpt-5.6` and one non-null Guard ruling.
- Show the Codex task ID once, legibly.
- Use voiceover throughout and upload a public or unlisted YouTube video.
